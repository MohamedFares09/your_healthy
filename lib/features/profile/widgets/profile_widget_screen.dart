import 'package:flutter/material.dart';

class ProfileWidgetScreen extends StatelessWidget {
  const ProfileWidgetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
